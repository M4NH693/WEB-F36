name: PHP Syntax Check Only

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  php-check:
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: ⚡ Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'

    - name: 🔬 PHP Syntax Check
      # Chỉ kiểm tra xem code PHP có viết sai cú pháp không
      # Bỏ qua hoàn toàn các lỗi HTML
      run: find . -name "*.php" ! -path "*/vendor/*" -print0 | xargs -0 -n1 php -l